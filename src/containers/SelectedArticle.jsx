import React from 'react';

const mapStateToProps = state => ({ selectedArticle });

class ConnectedSelectedArticle extends React.Component {
    render() {
        return (
            {
                selectedArticle?
                
                
            
            }
        )
    }
}
